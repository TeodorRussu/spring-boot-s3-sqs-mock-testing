create table entity
(
    id                                            BIGINT               NOT NULL,
    version                                       BIGINT               NOT NULL,
    date_created                                  TIMESTAMP            NOT NULL,
    PRIMARY KEY (id)
);
